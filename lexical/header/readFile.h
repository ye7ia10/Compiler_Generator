#ifndef READFILE_H_INCLUDED
#define READFILE_H_INCLUDED
#include <bits/stdc++.h>
#include <sstream>
#include <string>
#include <fstream>
using namespace std;

/* read file line by line.
   return vector of strings every string is a line */
vector<string>readFile(string fileName);

#endif // READFILE_H_INCLUDED
